{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aulasenas2.local/contracts/data-stages.schema.json",
  "title": "Registro por etapa de datos",
  "description": "DataStore persiste el mismo registro bajo raw, processed o validated y agrega id y stage. No hay evidencia actual de campos o transformaciones exclusivos de processed o validated.",
  "allOf": [
    { "$ref": "captured-sample.schema.json" },
    {
      "type": "object",
      "required": ["id", "stage"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "stage": { "enum": ["raw", "processed", "validated"] }
      }
    }
  ]
}
